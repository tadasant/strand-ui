{
  "name": "client",
  "version": "0.1.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "parcel-bundler": "^1.5.1"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --out-dir build/",
    "build staging": "parcel build index.html --no-minify --out-dir build/"
  }
}
